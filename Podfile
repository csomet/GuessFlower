# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'GuessFlower' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GuessFlower
  pod 'Alamofire', '~> 4.4'
  pod 'SwiftyJSON'
  pod 'SimpleAnimation'
  pod 'SDWebImage'
  pod "RSLoadingView"

end
